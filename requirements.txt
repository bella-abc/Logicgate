# Core dependencies for electricity price forecasting
torch>=2.0.0
numpy>=1.20.0
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
tqdm>=4.60.0
scipy>=1.7.0

# Optional: For advanced features (can be installed separately if needed)
# accelerate>=0.20.0
# transformers>=4.20.0
# einops>=0.6.0
# peft>=0.4.0
# deepspeed>=0.10.0
# sentencepiece>=0.1.99
